<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CalculatorInputEditTextStyle" parent="Base.Widget.AppCompat.EditText">
        <item name="android:layout_gravity">end</item>
        <item name="android:inputType">none</item>
        <item name="android:focusable">false</item>
        <item name="android:cursorVisible">false</item>
        <item name="android:ems">10</item>
        <item name="android:textAlignment">textEnd</item>
        <item name="android:textAppearance">?android:attr/textAppearanceLarge</item>
    </style>

    <style name="CalculatorButtonStyle" parent="Base.Widget.AppCompat.Button.Borderless">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="CalculatorOperationButtonStyle" parent="CalculatorButtonStyle">
        <item name="android:backgroundTint">@color/colorOutline</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

</resources>
